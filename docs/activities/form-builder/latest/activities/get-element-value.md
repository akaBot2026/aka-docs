---
id: get-element-value
title: "Get Element Value"
sidebar_label: "Get Element Value"
sidebar_position: 4
description: "Get Element Value activity documentation."
displayed_sidebar: activitiesSidebar
---

# Get Element Value

RCA.Activities.FormBuilder.GetElementValue

## **Description**
Retrieves the current value of a specific component (e.g., a text input, checkbox status, selected dropdown item) from an active form while the form remains open. This is primarily used inside the **Do** block of [Display Form](/docs/activities/form-builder/latest/activities/display-form.md) to dynamically inspect user entries and trigger conditional validation rules.

![get-element-value](/static/img/get-element-value.png)

---

## **In the body of the activity**
* **Key Name** - The unique key name of the form element from which you want to retrieve the value. This value must match the component's Property Name (Field Key) from the Form Builder layout, and must be enclosed in double quotes (e.g., `"txtAge"`).

---

## **Properties**

### **Input**
| Property Name | Data Type | Required | Default | Description & User-Centric Guide |
| :--- | :--- | :--- | :--- | :--- |
| **Form Id** | `InArgument<Int32>` | **Yes** | None | The ID of the form target. Bind this to the `Form Id` output variable generated by the parent [Display Form](/docs/activities/form-builder/latest/activities/display-form.md). |
| **Key Name** | `InArgument<String>` | **Yes** | None | The target component's Property Name (Field Key). Must be enclosed in double quotes (e.g. `"txtEmail"`). |

### **Output**
| Property Name | Data Type | Required | Description & User-Centric Guide |
| :--- | :--- | :--- | :--- |
| **Output Data** | `OutArgument<Object>` | **Yes** | The retrieved value stored in a workflow variable of type `Object`. You can cast or convert this variable later depending on the element type (e.g. using `Convert.ToString(value)` or `Convert.ToBoolean(value)`). |

### **Common**
| Property Name | Data Type | Required | Default | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Continue On Error** | `Boolean` | No | False | If `True`, the workflow continues running even if retrieving the element value fails. |

### **Misc**
| Property Name | Data Type | Required | Default | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Display Name** | `String` | No | Get Element Value | Name displayed in the workflow designer. |
| **Public** | `Boolean` | No | False | Check if you want to publish the activity execution logs. |

---

## **Step-by-Step Usage**
The **Get Element Value** activity operates on a live form instance, meaning it must be executed inside the **Do** block container. 

For example, to validate email formatting in real-time:
1. Drag a **Get Element Value** activity inside the **Do** block, configured for key `"txtEmail"`. Save the output to an Object variable `emailValue`.
2. Use an **Assign** activity to convert it to a string: `emailStr = Convert.ToString(emailValue)`.
3. Check the format using an **If** activity with a regex expression. If the format is invalid, use [Set Element Value](/docs/activities/form-builder/latest/activities/set-element-value.md) to set a warning message or disable the submit button.

---

## **Self-Service Troubleshooting**

### **Design-Time Validation**
* **Symptom**: Validation warning showing that `Output Data` is required.
  * **Cause**: You did not assign a variable to store the retrieved value.
  * **Solution**: Create a variable of type `System.Object` in your Variables panel and bind it to the **Output Data** field.

### **Run-Time Errors**
* **Symptom**: `InvalidCastException` when using the retrieved value in the workflow.
  * **Cause**: You attempted to directly assign or cast the `Object` output into an incompatible variable type (e.g., casting a text field's empty string directly into a boolean or integer variable).
  * **Solution**: Always convert or cast safely. Use `Convert.ToString(myValue)` for text, `Convert.ToBoolean(myValue)` for checkboxes, and `Convert.ToInt32(myValue)` for numbers, while checking for null values beforehand: `If(myValue IsNot Nothing, Convert.ToString(myValue), "")`.
* **Symptom**: `KeyNotFoundException: Component key 'xyz' not found`.
  * **Cause**: The key is misspelled or does not match any component's Field Key (Property Name) in the JSON schema.
  * **Solution**: Verify the key in the Form Builder UI designer.
