---
id: enable
title: "Enable"
sidebar_label: "Enable"
sidebar_position: 7
description: "Enable activity documentation."
displayed_sidebar: activitiesSidebar
---

# Enable

RCA.Activities.FormBuilder.Enable

## **Description**
Enables a previously disabled component (e.g., input field, dropdown, submit button) on an active form window. Once enabled, the component becomes active and fully interactive, allowing the end-user to type, select options, or click buttons. This activity works in tandem with the [Disable](/docs/activities/form-builder/latest/activities/disable.md) activity to control user input flows dynamically.

![enable](/static/img/enable.png)

---

## **In the body of the activity**
* **Key Name** - The unique key name of the form element you want to enable. This value must match the component's Property Name (Field Key) from the Form Builder layout, and must be enclosed in double quotes (e.g., `"btnSubmit"`).

---

## **Properties**

### **Input**
| Property Name | Data Type | Required | Default | Description & User-Centric Guide |
| :--- | :--- | :--- | :--- | :--- |
| **Form Id** | `InArgument<Int32>` | **Yes** | None | The ID of the form target. Bind this to the `Form Id` output variable generated by the parent [Display Form](/docs/activities/form-builder/latest/activities/display-form.md). |
| **Key Name** | `InArgument<String>` | **Yes** | None | The target component's Property Name (Field Key). Must be enclosed in double quotes (e.g. `"txtAddress"`). |

### **Common**
| Property Name | Data Type | Required | Default | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Continue On Error** | `Boolean` | No | False | If `True`, the workflow continues running even if enabling the component fails. |

### **Misc**
| Property Name | Data Type | Required | Default | Description |
| :--- | :--- | :--- | :--- | :--- |
| **Display Name** | `String` | No | Enable | Name displayed in the workflow designer. |
| **Public** | `Boolean` | No | False | Check if you want to publish the activity execution logs. |

---

## **Step-by-Step Usage**
The **Enable** activity must run inside the **Do** block of [Display Form](/docs/activities/form-builder/latest/activities/display-form.md) because it operates on a live form instance.

For example, to enable the submit button once the user has entered their name:
1. Drag a **Get Element Value** activity inside the **Do** block of `Display Form` to read the Full Name input field (`"txtFullName"`).
2. Place an **If** activity: check if the retrieved name is not empty.
3. If it is not empty, call **Enable** for the submit button key (`"btnSubmit"`) to allow form submission.
4. If it is empty, call [Disable](/docs/activities/form-builder/latest/activities/disable.md) for the `"btnSubmit"` key.

---

## **Self-Service Troubleshooting**

### **Design-Time Validation**
* **Symptom**: Studio displays a warning that `Key Name` or `Form Id` is required.
  * **Cause**: One of these properties is empty or the `Key Name` string lacks quotes.
  * **Solution**: Ensure `Key Name` is formatted as a string (e.g. `"btnSubmit"`) and `Form Id` is mapped to your `Form Id` variable.

### **Run-Time Errors**
* **Symptom**: `KeyNotFoundException: Component key 'xyz' not found` at runtime.
  * **Cause**: The key passed in **Key Name** (e.g. `"xyz"`) does not match any Field Key (Property Name) inside the JSON schema file.
  * **Solution**: Open the JSON schema file in Form Builder, verify the component's **Property Name** under the **Field Key** tab, and update the **Key Name** to match it exactly (case-sensitive).
* **Symptom**: `ArgumentException: Form ID does not exist`.
  * **Cause**: The form is already closed.
  * **Solution**: Ensure this activity is executed within the boundaries of the **Do** block execution loop.
